programa teste
inicio
	inteiro : i;
	const y 'hello world';

	i <- 2;
	i <- i+1;
	se (i = 3) entao
		i <- 'HUE';
	fim se;
	se (nao(i=3) xor nao(i=3)) entao
		z <- 2;
	fim se;

	/*
		Saída esperada: Erro1, tentativa de atribuir um array de char (linha 9) a uma variável inteira.
		Erro2, variável z não foi declarada. 
	*/

fim.