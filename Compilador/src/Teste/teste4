programa teste
inicio
	inteiro: c;
	real : a , b;

	c <- 2 ;
	a <- c;
	b <- 'Vire-se' ; // lembrar de corrigir


	/*
		Saída esperada: Erro, tentativa de atribuir um array de char (linha 8) a uma variável real.
	*/
fim.